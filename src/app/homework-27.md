1) Будет создан один экземпляр. 

2) Будет создан два экземпляра. 

3) parent component получит экземпляр. Потому что child пойдё к родителю перед тем как пойти к рутовому, а там в providers зарегистрирована зависимость. 

4) один экземпляр.

5) при первом inject. 

6) массив из объектов ['A', 'B'].

7) Приложение не ломается. Вернёт null. 

8) Выдвст ошибку. Не зарегстирован в компоненте. Self не даёт поднатся выше  element injector. 

9) родительский. 

10) выдаст ошибку no provaide, то есть null inject. 

11) один экземпляр. 

12) 1. Два экземпляра. 
    2. Element 
    3. root
    4. root
    5. у себя > родитель >  route >  root. 

13) Provider это инструкция кторый говорит inject как находить объект. сначало компонент смотрит у себя если там нету, смотрит в route если там нету, идёт к родителю, ести там нету идёт в root и если там нету то null inject.  
При вызове inject (A), Angular смотрит в память, если там нету этого объекта, то начинает создание этого объекта и видит что этот объект вызывает объект (B) и Angular снова смотрит в память и если его там нет, то начинает его создание. При его создани видит что этот объект вызывает объект (C) и смотрит в память и если его нет, то начинает его создание и видит что он вызывает D и начинает его создание, если его нет в памяти. А при создании объекта D Angular видит что он вызывает Loger Service b смотрит есть ли он в памяти, если есть берёт оттуда, а если нету также начинает его создане. При успешном содании всех этих объектов, всe объекты которых не было в памяти, кэшируется в память Anhgular. 

14) Пока сервис не используется в памяти ничего нету. Потом при вызове inject user service Angular создаёт этот объект который в свою очередь исползует api sevice и Angular снова создаёт объект. При создании api service Angular снова видит что ему нужен Logger service и создаёт объект. В итоге в памяти будет три объекта. 

15) 1. ApiService - root, 
    2. AuthService - root, 
    3. CartService - root,
    4. ProductFiletrService - element injector,
    5. NotificationService - root,
    6. ThemeService - root,
    7. DashbordStatisticsService - element injector,
    8. UserTableStateService - rout injector,
    9. ModalService - root,
    10. LoggerService - element injector,
    11. AppConfig - InjectTocen,
    12. CurrencyFormatter - useValue,
    13. AnalyticsService - useFactory
    